{
  "policies": [
    {
      "handler": {
        "export": "RateLimitInboundPolicy",
        "module": "$import(@zuplo/runtime)",
        "options": {
          "rateLimitBy": "function",
          "requestsAllowed": 3,
          "timeWindowMinutes": 1,
          "identifier": {
            "module": "$import(./modules/bot-id-and-tls)",
            "export": "rateLimitKey"
          }
        }
      },
      "name": "rate-limit-inbound-2",
      "policyType": "rate-limit-inbound"
    },
    {
      "handler": {
        "export": "RateLimitInboundPolicy",
        "module": "$import(@zuplo/runtime)",
        "options": {
          "rateLimitBy": "function",
          "requestsAllowed": 1000,
          "timeWindowMinutes": 1,
          "identifier": {
            "module": "$import(./modules/graphql-operation-limiter)",
            "export": "rateLimitKey"
          }
        }
      },
      "name": "rate-limit-graphql",
      "policyType": "rate-limit-inbound"
    },
    {
      "handler": {
        "export": "RateLimitInboundPolicy",
        "module": "$import(@zuplo/runtime)",
        "options": {
          "rateLimitBy": "function",
          "requestsAllowed": 1000,
          "timeWindowMinutes": 1,
          "identifier": {
            "module": "$import(./modules/global-bot-score-rate-limiter)",
            "export": "rateLimitKey"
          }
        }
      },
      "name": "rate-limit-bot-score",
      "policyType": "rate-limit-inbound"
    },
    {
      "handler": {
        "export": "default",
        "module": "$import(./modules/overall-rate-bot-score-policy)",
        "options": {}
      },
      "name": "overall-rate-bot-score",
      "policyType": "custom-code-inbound"
    }
  ]
}
